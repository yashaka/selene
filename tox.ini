[tox]
envlist = py{27,35}
recreate = True

[testenv]
envdir = {toxinidir}/.env
passenv = DISPLAY
basepython =
    py27: python2.7
    py35: python3.5
deps =
    -r{toxinidir}/requirements.txt
    -r{toxinidir}/test-requirements.txt
    pytest-cov
commands = py.test -sv --cov-report html --cov-report term --cov=selene --tb=short tests/